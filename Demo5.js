"use strict"
var num1 = 34;

var num2 = 3.1;

var resultingNumber = num1 + num2;

// console.log(resultingNumber);

// resultingNumber = num1 * num2;


resultingNumber = Math.pow(num1, num2); // Math.pow(7,4) is the oldest version of exponent. The new version is using 7**4.

console.log(resultingNumber);